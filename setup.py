"""Dummy setup file for editable installation."""
from setuptools import setup
from setuptools.extension import Extension

# add dummy extension module to force setuptools to build a os-specific wheel
setup()
